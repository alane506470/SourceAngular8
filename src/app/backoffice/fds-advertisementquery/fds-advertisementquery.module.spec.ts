import { FdsAdvertisementqueryModule } from './fds-advertisementquery.module';

describe('FdsAdvertisementqueryModule', () => {
  let fdsAdvertisementqueryModule: FdsAdvertisementqueryModule;

  beforeEach(() => {
    fdsAdvertisementqueryModule = new FdsAdvertisementqueryModule();
  });

  it('should create an instance', () => {
    expect(fdsAdvertisementqueryModule).toBeTruthy();
  });
});
