import { FileUploaderCustom } from './file-uploader-custom';

describe('FileUploaderCustom', () => {
  it('should create an instance', () => {
    expect(new FileUploaderCustom()).toBeTruthy();
  });
});
